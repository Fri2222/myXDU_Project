# Codex 交接摘要

本文档用于向下一个 Codex 说明本项目此前对话中的关键约束、已完成工作、当前状态和待继续处理事项。

## 1. 用户长期约束与偏好

- 参考文献植入必须只允许使用 `reference.bib` 中已有条目。
- 允许读取 `文献插入提示.txt` 作为辅助线索，但若与 `reference.bib` 冲突，必须以 `reference.bib` 为准。
- 文献植入必须进行严格语义对齐，不能在逻辑关联弱的地方强行加引文。
- 对正文几乎没有重写权限。最初用户要求仅在句末添加 `\cite{键值}`，后续对第三章特别强调“引用放在标点前”。
- 不修改致谢。
- 不修改第五章的“结论和结束语”。
- 尽量保留原有结构，只修正必要内容。
- 关键词需要修改。
- 会议类文献和 arXiv 文献要按学术界常见格式处理，并与期刊类在形式上保持统一，但不是把它们硬改成期刊格式。
- 忽略装订要求。
- 需要修改字体要求和标题版式。
- 用户多次要求先检查、再报告、得到命令后再改；但也有明确直接执行修改的回合，需要按当轮指令执行。

## 2. 参考文献相关工作

- 已为第一章、第三章、第四章补入参考文献。
- 文献植入遵循用户要求，优先从 `reference.bib` 中选择已有键值。
- 第三章文献植入时，注意了“引用放在标点符号前”这一要求。
- 用户指出参考文献目录不规范后，已对参考文献规范问题进行过处理方向讨论，并进一步要求：
  - 修改所有 `@online` 条目。
  - 会议类和 arXiv 类条目也按规范统一处理。
- `reference.bib` 当前已被修改过。
- 目前 `biber` 仍提示一个重复键问题：
  - `songMotionEstimationMultiObject2025`
  - 下一个 Codex 应检查 `reference.bib` 中该键是否重复定义，并决定如何消解。

## 3. 摘要与关键词相关工作

- 用户曾要求解决 `chapters/abstract-zh.tex` 的 PDF 预览不显示问题。
- 用户随后要求将中文摘要改为更简洁、约 300-400 字、符合学术规范。
- 用户明确要求将关键词修改为：
  - `神经网络`
  - `卡尔曼滤波`
  - `多目标跟踪`
- 摘要与关键词相关内容已经历过修改请求；后续若继续调整，应保持这一关键词要求。

## 4. 格式规范与语言检查相关工作

- 用户要求依据以下文件检查格式规范：
  - `D:\22017\Documents\毕业设计\myXDU_Project\19.西安电子科技大学本科毕业设计（论文）撰写规范.docx`
- 用户要求先报告哪里不规范，不立即修改。
- 后续用户进一步限定：
  - 不用修改致谢。
  - 不修改第五章结论和结束语。
  - 修改关键词。
  - 忽略装订要求。
  - 修改字体要求和标题版式。
- 用户还要求检查全部文档的语言错误，只指出位置和修改建议，先不要改。
- 随后用户又要求：
  - 在保留原结构前提下，先修正英文表达、错别字和全角/半角标点符号。
- 用户提供过一个格式检测报告：
  - `D:\22017\Documents\毕业设计\myXDU_Project\main_格式检测报告.html`
- 用户要求依据该检测报告进行修改。

## 5. 图片重构与美化相关工作

- 用户要求重构 `figures` 目录下序号为 7、8、9、31、32 的图片。
- 要求输出新的 SVG 和 PNG，并生成对应 Python 代码。
- 用户要求保留原图流程和文字描述，可优化文字大小、配色、框位置等；如必要可调整流程，但语义应保持相近。
- 后续又提出多轮细化要求：
  - 修复 `31.系统流程图` 中箭头压到文字的问题。
  - 修复 `32.KalmanNet架构-横向排版` 中文字脱离文本框的问题。
  - 数学表达要正确显示，如 `F_2` 要显示成下标形式。
  - 将 `31.系统流程图` 中 “KalmanNet_bytetrack跟踪核” 改为 “bytetrack算法”。
  - 将 `31.系统流程图` 中轨迹管理到系统输出的两段箭头改为横平竖直的一段直箭头。
  - 删除 `31.系统流程图` 中由 YOLOX 指向置信度划分的箭头。
- 当前与图片重构相关的重要文件包括：
  - `chapters/rebuild_figure_assets.py`
  - `figures/31.系统流程图.png`
  - `figures/31.系统流程图.svg`
  - `figures/32.KalmanNet架构-横向排版.png`
  - `figures/32.KalmanNet架构-横向排版.svg`
  - `figures/7.*`
  - `figures/8.*`
  - `figures/9.*`
- `git status` 显示上述多张图片和脚本文件目前处于已修改或未跟踪状态。

## 6. 第四章实验部分的近期修改

### 6.1 在 4.3.2 节插入图 17 及说明

- 用户要求在 `4.3.2` 节加入图片：
  - `17.训练序列长度对性能的影响-架构2`
- 已在文件中插入：
  - 文件：`chapters/ch04-experiments.tex`
  - 图标签：`fig:arch2_seq_length`
  - 图题：`架构 2 中训练序列长度对性能的影响`
- 已补入与上下文衔接的分析段落，核心含义是：
  - 架构 2 在序列长度 15-35 帧时性能较稳定；
  - 当长度增至 40 帧及以上时，`MOTA` 明显下降；
  - 解释为该架构直接预测完整卡尔曼增益，长序列下噪声与误差更易累积。

### 6.2 修复图 41 的错误引用

- 之前第四章中错误引用了不存在的图片名：
  - `41.增益限制对性能的影响-架构三响`
- 实际文件为：
  - `figures/41.增益限制对性能的影响-架构三.png`
- 现已在 `chapters/ch04-experiments.tex` 中修正为正确文件名。
- 用户曾贴出大量编译报错，根因就是这一处图片名错误；其他大量 `undefined reference/citation` 多数是连带现象。

## 7. 第三章系统流程图插入工作

- 用户要求“找合适的位置将 `31.系统流程图` 插入，并补充说明，保持上下文通顺”。
- 已在第三章系统总体架构设计部分插入该图：
  - 文件：`chapters/ch03-system-architecture.tex`
  - 图题：`系统整体处理流程`
  - 标签：`fig:system_pipeline`
- 已补入两段承接说明，主要内容包括：
  - YOLOX 输出候选框并按置信度划分；
  - ByteTrack 进行高低置信度两轮关联；
  - KalmanNet 嵌入运动预测/增益修正环节；
  - 轨迹管理维护新建/活跃/丢失轨迹并输出多目标跟踪结果。

## 8. 编译状态

- 最近已多次使用 `xelatex` 编译 `main.tex`。
- 至少有一轮成功编译生成 `main.pdf`，并且在最近一次加入第三章流程图后再次成功编译。
- 当前 `main.pdf` 可以生成。
- 最近一次编译后仍存在的已知警告：
  - `fig:arch_distribution` 标签重复定义。
  - 一些字体告警，例如 `CambriaMath` 粗体替代。
  - 少量 `Overfull/Underfull` 警告。
- 这些问题不是最近一次插图操作新引入的致命错误。

## 9. 当前工作区状态

根据最近一次 `git status --short`，项目存在多处未提交修改，包括但不限于：

- `reference.bib`
- `main.bbl`
- `main.blg`
- `main.pdf`
- `figures/7...png`
- `figures/8...png`
- `figures/9...png`
- `figures/31.系统流程图.png`
- `figures/31.系统流程图.svg`
- `figures/32.KalmanNet架构-横向排版.png`
- `figures/32.KalmanNet架构-横向排版.svg`
- `figures/41.增益限制对性能的影响-架构三.png`
- `chapters/rebuild_figure_assets.py`
- `figures/rebuild_figure_assets.py`
- `main_格式检测报告.html`

下一个 Codex 接手时，必须避免误回滚这些已有改动。

## 10. 建议下一个 Codex 优先核对的事项

1. 先读本文件，再查看 `git status`，确认哪些改动已经存在，避免重复劳动。
2. 检查 `reference.bib` 中重复键 `songMotionEstimationMultiObject2025`。
3. 检查 `fig:arch_distribution` 为何重复定义。
4. 若继续做论文规范修订，优先遵守用户的排除项：
   - 不改致谢。
   - 不改第五章结论和结束语。
5. 若继续处理图片，优先基于已有的 `rebuild_figure_assets.py` 和已重构 SVG/PNG 资产继续，而不是从头重做。
6. 若继续处理文献格式，务必坚持只使用 `reference.bib` 内部条目，不引入外部文献键。

